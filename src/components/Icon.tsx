"use client";

const Icon = () => {
  return (
    <div
      style={{
        fontWeight: 700,
        fontSize: "14px",
        color: "#000",
        width: 32,
        height: 32,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      OD
    </div>
  );
};

export default Icon;
