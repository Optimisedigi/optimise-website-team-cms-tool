"use client";

const Icon = () => {
  return (
    <img
      src="/optimise-digital-logo-white.png"
      alt="Optimise Digital"
      style={{
        maxWidth: 180,
        height: "auto",
        objectFit: "contain",
      }}
    />
  );
};

export default Icon;
