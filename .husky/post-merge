MERGED_BRANCH=$(git reflog -1 | grep -oP "merge \K[^:]+")
if [ -n "$MERGED_BRANCH" ] && [ "$MERGED_BRANCH" != "main" ]; then
  git branch -d "$MERGED_BRANCH" 2>/dev/null && echo "Cleaned up branch: $MERGED_BRANCH"
fi
