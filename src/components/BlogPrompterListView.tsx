import BlogPrompterPage from './BlogPrompterPage'

export default function BlogPrompterListView() {
  return (
    <div className="gutter--left gutter--right" style={{ maxWidth: 1100 }}>
      <BlogPrompterPage />
    </div>
  )
}
