"use client";

const Icon = () => {
  return (
    <img
      src="/optimise-rocket-logo-black.png"
      alt="Optimise Digital"
      style={{
        maxWidth: 25,
        height: "auto",
        objectFit: "contain",
      }}
    />
  );
};

export default Icon;
