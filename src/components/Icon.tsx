"use client";

const Icon = () => {
  return (
    <img
      src="/optimise-rocket-logo-white.webp"
      alt="Optimise Digital"
      style={{
        maxWidth: 200,
        height: "auto",
        objectFit: "contain",
      }}
    />
  );
};

export default Icon;
